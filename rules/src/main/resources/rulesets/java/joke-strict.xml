<?xml version="1.0" encoding="UTF-8"?>

<ruleset name="joke-strict"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

    <description>
        The whole analysis this project runs on itself: PMD's stock categories, composed with the
        exclusions and property overrides that make them workable, plus every rule this artifact
        defines. One reference, no composition to maintain.

        Requires PMD 7.26.0 or later. This floor is higher than the 7.0.0 floor carried by the rule
        classes, by category/java/joke.xml and by rulesets/java/joke.xml. Unlike those, this file
        names rules it does not own — a stock rule renamed or removed by a later PMD is a
        ruleset-load failure here, and at least one name it excludes (ImplicitFunctionalInterface)
        did not exist in 7.0.0. This floor is the version range the project supports and analyses
        itself with; it is not asserted by a build check.

        On a PMD older than that, or to compose the stock categories differently, reference
        rulesets/java/joke.xml instead and bring your own composition. Gradle's `ruleSets` cannot
        subtract from a ruleset, so wanting this composition minus one rule means writing your own
        ruleset file that references this one and excludes from it.
    </description>

    <rule ref="category/java/bestpractices.xml">
        <exclude name="GuardLogStatement"/>
        <exclude name="ImplicitFunctionalInterface"/>
    </rule>
    <rule ref="category/java/codestyle.xml">
        <exclude name="OnlyOneReturn"/>
        <exclude name="CommentDefaultAccessModifier"/>
        <exclude name="ShortClassName"/>
        <exclude name="ShortMethodName"/>
        <exclude name="LongVariable"/>
        <exclude name="ShortVariable"/>
        <exclude name="UseExplicitTypes"/>
        <exclude name="AtLeastOneConstructor"/>
        <!-- In direct opposition to UseStaticImports, which this artifact defines: that rule only
             ever adds static imports and this one caps them at four, so no configuration satisfies
             both. Imports are cheap and the IDE folds them away; a cap buys nothing and costs the
             readability it claims to protect. -->
        <exclude name="TooManyStaticImports"/>
    </rule>

    <rule ref="category/java/design.xml">
        <exclude name="LawOfDemeter"/>
        <exclude name="CouplingBetweenObjects"/>
        <exclude name="ExcessiveImports"/>
        <exclude name="TooManyMethods"/>
        <exclude name="LoosePackageCoupling"/>
        <exclude name="CognitiveComplexity"/>
        <exclude name="CyclomaticComplexity"/> <!-- we prefer CognitiveComplexity -->
        <exclude name="AvoidDeeplyNestedIfStmts"/>
        <exclude name="NPathComplexity"/>
    </rule>
    <rule ref="category/java/design.xml/CognitiveComplexity">
        <properties>
            <property name="reportLevel" value="5" />
        </properties>
    </rule>
    <rule ref="category/java/design.xml/AvoidDeeplyNestedIfStmts">
        <properties>
            <property name="problemDepth" value="2" />
        </properties>
    </rule>
    <rule ref="category/java/design.xml/NPathComplexity">
        <properties>
            <property name="reportLevel" value="5" />
        </properties>
    </rule>

    <rule ref="category/java/errorprone.xml">
        <exclude name="UseProperClassLoader"/>
    </rule>
    <rule ref="category/java/multithreading.xml"/>
    <rule ref="category/java/performance.xml">
        <exclude name="AvoidInstantiatingObjectsInLoops"/>
    </rule>

    <rule ref="rulesets/java/joke.xml"/>

</ruleset>
